services:
  front:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    container_name: pool-front
    ports:
      - "5173:5173"
    volumes:
      - ./:/app
      - /app/node_modules
